[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "SIM"]
ignore = [
    "B008",
]

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
exclude = ["team_reports/tests/e2e"]
